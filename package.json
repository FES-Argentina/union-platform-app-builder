{
  "name": "com.unionplatform.test",
  "displayName": "Union Platform",
  "version": "1.0.0",
  "description": "Plataforma open-source para sindicatos",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "emulate": "env-cmd cordova run --emulate",
    "build-debug": "env-cmd cordova build android",
    "build-release": "env-cmd cordova build android --release -- --packageType=bundle",
    "build-release-signed": "env-cmd cordova build android --release"
  },
  "keywords": [
    "ecosystem:cordova"
  ],
  "author": "FUNDACIÓN FRIEDRICH EBERT",
  "license": "Apache-2.0",
  "cordova": {
    "platforms": [
      "android"
    ]
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "env-cmd": "^10.1.0"
  },
  "devDependencies": {
    "cordova-android": "^10.1.1"
  }
}
